*** Все пустые значения из таблицы разбора в файле "table.txt" заполнять как -1
В первой строке размер таблицы

*** В файле "terms" в первой строке записаны все терминалы через пробел, во второй нетерминалы, важно учитывать порядок,
такой же как в таблице разбора

*** В файле "rules.txt" все правила записаны в порядке их нумерации и на каждое правило использовать строку (без '|')
В первой строке указано кол-во правил